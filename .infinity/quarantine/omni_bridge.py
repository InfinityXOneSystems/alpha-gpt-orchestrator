import logging

        logging.info(f'[OMNI ROUTER] {source} -> {target}')
    def route(self, source, target, payload):
        logging.info(f'[OMNI ROUTER] {source} -> {target}')
        return payload
