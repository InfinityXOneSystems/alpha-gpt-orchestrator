name: Infinity Autonomy Tick

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  tick:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Tick
        env:
          ORCH_URL: ${{ secrets.ORCH_URL }}
        run: |
          curl -X POST "$ORCH_URL/echo/tick"
